[build]
  base    = "frontend/invoicemanagement-app"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# SPA routing — send all paths to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
